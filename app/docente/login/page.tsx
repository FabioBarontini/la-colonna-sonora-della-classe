"use client";
import { Suspense, useState } from "react"; import { useRouter,useSearchParams } from "next/navigation"; import Link from "next/link"; import { LockKeyhole, Music2 } from "lucide-react";
function LoginForm(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[err,setErr]=useState(""),[loading,setLoading]=useState(false);const router=useRouter(),params=useSearchParams();async function go(e:React.FormEvent){e.preventDefault();setLoading(true);setErr("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(r.ok)router.push(params.get("next")||"/docente/classe");else{const d=await r.json();setErr(d.error||"Credenziali non valide.");}setLoading(false)}return <main className="min-h-screen py-10"><div className="container flex min-h-[85vh] items-center justify-center"><div className="glass w-full max-w-md p-7 md:p-9"><Link href="/" className="inline-flex items-center gap-2 text-sm muted hover:text-white"><Music2 size={16}/> Torna alla home</Link><div className="mt-12 flex h-14 w-14 items-center justify-center rounded-2xl bg-violet-400/10 text-violet-300"><LockKeyhole/></div><div className="eyebrow mt-8">Area riservata</div><h1 className="mt-2 text-4xl font-black">Accesso docente</h1><p className="mt-3 leading-6 muted">Dashboard, playlist, mappa musicale e gioco della classe.</p><form onSubmit={go} className="mt-8 space-y-4"><label className="block text-sm font-medium">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="nome@scuola.it" className="field mt-2"/></label><label className="block text-sm font-medium">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="field mt-2"/></label>{err&&<p className="rounded-xl bg-rose-400/10 px-4 py-3 text-sm text-rose-200">{err}</p>}<button disabled={loading} className="btn-primary w-full">{loading?"Accesso…":"Entra nell'area docente"}</button></form></div></div></main>}
export default function Login(){return <Suspense fallback={<div className="min-h-screen"/>}><LoginForm/></Suspense>}
